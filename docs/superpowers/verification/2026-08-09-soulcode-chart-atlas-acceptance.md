# SoulCode 图谱展示本地验收记录

日期：2026-08-09

范围：人类图、八字四柱、紫微斗数、五运六气四个展示组件；不含线上部署。

## 自动化结果

| 检查项 | 结果 | 记录 |
|---|---|---|
| Vitest 全量测试 | 通过 | 22 个测试文件，66 个测试通过 |
| TypeScript / Next.js 生产构建 | 通过 | 55 个路由生成成功 |
| `git diff --check` | 通过 | 无空白错误 |
| 临时预览页清理 | 通过 | `src/app/chart-preview/page.tsx` 已删除 |

构建仍有既有的 metadata viewport 警告，未阻断构建，也不是本轮图表改版引入的问题。

## 视觉结果

| 视口 | 结果 | 记录 |
|---|---|---|
| 桌面 1280×900 | 通过 | 四图比例稳定，图表文字无重叠，页面无横向滚动 |
| 手机 390×844 | 通过 | 四图自动单列，固定导航不遮挡图表，页面宽度为 390px |
| A4 打印预览 | 局部通过 | SVG 清晰、中文可读、四图完整；临时预览页仍带有站点壳层元素，需在真实人生总览 PDF 上复验 |

## 已修复的明显问题

1. 人类图从深色小卡改为浅底原创 SVG，中心名称移到图形外侧，避免与闸门数字重叠。
2. 八字改为四柱立柱结构，日主突出，五行改为底部比例摘要。
3. 紫微十二宫改为外环布局，中央命盘摘要不再与命宫宫格重叠。
4. 五运六气改为双指标摘要卡，去除大面积空白。
5. 桌面两列、手机单列，保留现有排盘数据和报告下载入口。

## 未完成或需后续复验

- 本地真实人生总览排盘等待 180 秒仍未完成，未能在本轮拿到完整 AI 报告页面。该现象属于报告生成链路耗时问题，不能据此宣称报告生成速度已验收。
- 整份实际报告 PDF 仍需在 AI 报告成功生成后复验，包括正文、图表、下载和打印壳层；本轮只完成了四图局部打印验证。
- 页面仍显示 Next.js 的 metadata viewport 警告，建议后续单独整理，不与图表改版混修。

结论：四个图表的本地视觉改版达到“可继续进入真实报告验收”的状态；尚未达到“完整报告链路全部验收完成”，因此本轮不部署线上。
